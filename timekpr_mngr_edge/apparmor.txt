#include <tunables/global>

profile %%SLUG%% flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Capabilities
  capability fowner,
  capability chown,

  # Signals (Corrected)
  signal (send) set=(kill,term,int,hup,cont),

  # Networking
  network inet stream,
  network inet6 stream,

  # Files (Broad access for testing, refine later)
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /data/** rw,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio and Temporary Files
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Application Code and Runtime Updates
  # Allows 'git pull' and 'pip install' to update the code
  /usr/bin/python3* ix,
  /usr/bin/git* ix,
  /usr/bin/ssh* ix,
  /app/** rwix,
  /usr/lib/python3*/** rw,
  /usr/local/lib/python3*/** rw,

  # Data and Persistent Storage
  # Required for SSH keys and the /app/instance symlink
  /data/** rw,
  /share/** rw,
}
