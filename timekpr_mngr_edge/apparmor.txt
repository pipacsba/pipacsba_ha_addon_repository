#include <tunables/global>

profile %%SLUG%% flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Capabilities
  capability fowner,
  capability chown,
  capability dac_override,

  # Signals
  signal (send) set=(kill,term,int,hup,cont),

  # Networking
  network inet stream,
  network inet6 stream,

  # Core System Paths
  # Added 'r' to /init to allow sh to read the entrypoint script
  /init rix,
  /bin/** rix,
  /usr/bin/** rix,
  /lib/** rm,
  /usr/lib/** rm,

  # s6-overlay v3 Requirements
  /run/{s6,s6-rc*,service}/** rix,
  /package/** rix,
  /command/** rix,
  /etc/services.d/** rix,
  /etc/cont-init.d/** rix,
  /etc/cont-finish.d/** rix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio and Temporary Files
  /usr/lib/bashio/** rix,
  /tmp/** rwk,

  # Application Code and Runtime Updates
  /usr/bin/python3* rix,
  /usr/bin/git* rix,
  /usr/bin/ssh* rix,
  /app/** rwix,
  /usr/lib/python3*/** rw,
  /usr/local/lib/python3*/** rw,

  # Data and Persistent Storage
  /data/** rw,
  /share/** rw,
  /config/** rw,
}
