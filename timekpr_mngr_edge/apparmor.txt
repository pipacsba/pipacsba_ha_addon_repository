#include <tunables/global>

profile %%SLUG%% flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Capabilities
  capability fowner,
  capability chown,
  capability dac_override,
  capability dac_read_search,

  # Signals
  signal (send) set=(kill,term,int,hup,cont),

  # Networking
  network inet stream,
  network inet6 stream,

  # Core System Paths
  /init rix,
  /bin/** rix,
  /usr/bin/** rix,
  /lib/** rm,
  /usr/lib/** rm,

  # s6-overlay v3 Requirements (The Fix)
  # Allow reading the service definitions and the s6-overlay scripts
  /etc/s6-overlay/** r,
  /usr/lib/s6/** rix,
  /bin/s6-* rix,
  /usr/bin/s6-* rix,
  
  # Legacy/standard s6 paths
  /run/{s6,s6-rc*,service}/** rix,
  /package/** rix,
  /command/** rix,
  /etc/services.d/** rix,
  /etc/cont-init.d/** rix,
  /etc/cont-finish.d/** rix,

  # Runtime and Temp
  /run/{,**} rwk,
  /tmp/** rwk,
  /dev/tty rw,

  # Bashio and Application Code
  /usr/lib/bashio/** rix,
  /usr/bin/python3* rix,
  /usr/bin/git* rix,
  /usr/bin/ssh* rix,
  /app/** rwix,
  /usr/lib/python3*/** rw,
  /usr/local/lib/python3*/** rw,

  # Data and Persistent Storage
  /data/** rw,
  /share/** rw,
  /config/** rw,
}
