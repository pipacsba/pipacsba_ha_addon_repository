#include <tunables/global>

profile timekpr_mngr_edge flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/python>
  #include <abstractions/openssl>

  # Capabilities
  capability signal,
  signal (send) set=(kill,term,int,hup,cont),

  # Networking (Web server, SSH, and MQTT)
  # Allows TCP connections for Ingress (port 5002), SSH, and MQTT
  network inet stream,
  network inet6 stream,

  # S6-Overlay and Core System
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio and Temporary Files
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Python Environment and Application Code
  # Required to run 'python /app/main.py'
  /usr/bin/python3* ix,
  /app/** r,
  /usr/lib/python3*/** r,
  /usr/local/lib/python3*/** r,

  # Data and Persistent Storage
  # Required for the /app/instance symlink and SSH keys
  /data/** rw,
  /share/** rw,

  # Specific access for managed SSH keys
  /data/ssh_keys/** rw,
}
